name: Deploy via ftp
on: push
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Upload ftp
      uses: sebastianpopp/ftp-action@v2.0.0
      with:
        host: ${{ secrets.HOST }}
        user: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASS }}
        localDir: ""
        remoteDir: "/pruebas-deploy"
        options: "--delete --asci"